<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wycliffe.com.myretrofit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/retro_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.Welcome"></activity>
    </application>

</manifest><!--
    THE PROCESS

     1) Creating a Models :
                - Get sample JSON data
                - Map the JSON data to Java (JsonSchema2pojo
                - Import data model to android studio

     2) Create a Retrofit instance - to send network request to an API, using the RetrofitBuilder.
     3) Defining the endpoints - defined inside of an interface using special retrofit
        annotations to encode details about the parameters and request method.
     4) Make the request.
     5)
-->
